# chamberlingate.com — placeholder site

A single static placeholder page for **Chamberlin Gate**, served via GitHub Pages.

- `index.html` — the page (self-contained: no external scripts, fonts, or images).
- `CNAME` — the custom domain GitHub Pages should serve (`chamberlin-gate.com`).

**Status:** placeholder. The page states the method is public domain and the tool is
open (Apache-2.0, planned) with the name reserved. A human review is required before DNS is
pointed at this repo.

**Do not add:** analytics, third-party embeds, generated/AI artwork, or any contact detail
that has not been approved.
